server:
  port: 80
spring:
  datasource:
    url: jdbc:mysql://生产IP:3306/smart_shop?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: prod_user
    password: prod_pwd

  data:
    redis:
      host: 生产RedisIP
      port: 6379
      database: 0

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true