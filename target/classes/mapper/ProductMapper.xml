<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="guat.lxy.mapper.ProductMapper">
    <select id="selectProductByCondition" resultType="guat.lxy.entity.Product">
        SELECT p.*, c.name AS category_name
        FROM product p LEFT JOIN category c ON p.cat_id = c.id
        <where>
            <if test="catId != null">p.cat_id = #{catId}</if>
            <if test="name != null and name != ''">AND p.name LIKE CONCAT('%',#{name},'%')</if>
            <if test="minPrice != null">AND p.price >= #{minPrice}</if>
            <if test="maxPrice != null">AND p.price &lt;= #{maxPrice}</if>
        </where>
    </select>

    <update id="update" parameterType="guat.lxy.entity.Product">
        UPDATE product
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="photoUrl != null">photo_url = #{photoUrl},</if>
            <if test="price != null">price = #{price},</if>
            <if test="descp != null">descp = #{descp},</if>
            <if test="releaseDate != null">release_date = #{releaseDate},</if>
            <if test="catId != null">cat_id = #{catId},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>